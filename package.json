{
  "name": "bot-replit-safe",
  "version": "1.0.0",
  "private": true,
  "description": "Safe group moderation bot core with a platform adapter boundary.",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "sync": "node scripts/sync-to-github.mjs"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "engines": {
    "node": ">=20"
  }
}
